# ==========================================
# 🚜 SIGMA LOC10 | CONFIGURAÇÃO DE AMBIENTE
# Copie este arquivo para .env na raiz antes de rodar o Docker
# ==========================================

# --- 🐘 DATABASE (Postgres) ---
# Credenciais usadas tanto pelo Docker Compose quanto pelo Django
DB_NAME=loc10
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=db
DB_PORT=5432

# URL de Conexão Completa (Usada pelo dj-database-url no Django)
DATABASE_URL=postgres://postgres:postgres@db:5432/loc10

# --- 🐍 DJANGO (Backend) ---
# Chave insegura apenas para desenvolvimento local
SECRET_KEY=django-insecure-dev-key-public-showcase
DEBUG=True

# Hosts permitidos (backend é o nome do container, api é alias comum)
ALLOWED_HOSTS=localhost,127.0.0.1,backend,api,*

# --- ⚡ REDIS (Cache & Queue) ---
REDIS_URL=redis://redis:6379/0

# --- ⚛️ NEXT.JS (Frontend) ---
# URL da API acessível pelo navegador do usuário
NEXT_PUBLIC_API_URL=http://localhost:8000/api